import _ from "lodash";
const fs = require( "fs" );

const moment = require( "moment" );

fs.readFileSync( "./text.txt" );

function test () {
	console.log( "test" );
}

_.isUndefined();
_.cloneDeep;
